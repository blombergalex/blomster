import { postType } from "@/app/utils/types"
import { Card } from "@nextui-org/react"


const postComponent = ({title, content, imageSrc}: postType) => {
  return (
    <Card />
  )
}

export default postComponent